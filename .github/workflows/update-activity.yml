name: Update Repo List
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/user-repository-list@master
        with:
          # Use your specific secret name
          token: ${{ secrets.GH_PAT_TRACK_ACTIVITIES }}
          # This ensures private repos you own are included
          type: owner
          # Shows the top 5 most recently updated
          limit: 5
          sort: updated
